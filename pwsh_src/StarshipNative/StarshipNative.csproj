<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <AssemblyName>StarshipNative</AssemblyName>
    <RootNamespace>StarshipNative</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>

  <!-- Ship the module manifest + script module next to the binary so the
       dotnet output directory is a complete, importable module (also used by
       the CMake install rule and by Windows packaging). -->
  <ItemGroup>
    <None Include="..\starship-native.psd1" Link="starship-native.psd1"
          CopyToOutputDirectory="PreserveNewest" />
    <None Include="..\starship-native.psm1" Link="starship-native.psm1"
          CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
